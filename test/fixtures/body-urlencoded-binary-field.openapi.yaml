openapi: 3.0.3
info: { title: Urlencoded Binary, version: 1.0.0 }
paths:
  /x:
    post:
      operationId: postX
      tags: [x]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                avatar: { type: string, format: binary }
      responses:
        '200':
          description: ok
          content:
            application/json: { schema: { type: object } }
