openapi: 3.0.3
info: { title: Multipart Non Object, version: 1.0.0 }
paths:
  /x:
    post:
      operationId: postX
      tags: [x]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: string
      responses:
        '200':
          description: ok
          content:
            application/json: { schema: { type: object } }
