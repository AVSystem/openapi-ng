openapi: 3.0.3
info:
  title: Cookie Param
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      tags:
        - Pet
      parameters:
        - name: sessionId
          in: cookie
          required: true
          schema:
            type: string
      responses:
        '200':
          description: ok
