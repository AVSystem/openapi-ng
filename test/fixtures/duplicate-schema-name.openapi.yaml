openapi: 3.0.3
info:
  title: Dup Schema
  version: 1.0.0
paths: {}
components:
  schemas:
    Pet:
      type: object
      properties: { id: { type: string } }
    Pet:
      type: object
      properties: { name: { type: string } }
