openapi: 3.0.3
info: { title: Urlencoded Nested, version: 1.0.0 }
paths:
  /x:
    post:
      operationId: postX
      tags: [x]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                metadata:
                  type: object
                  properties:
                    authorId: { type: string }
      responses:
        '200':
          description: ok
          content:
            application/json: { schema: { type: object } }
