openapi: 3.0.3
info: { title: Multipart Open Schema, version: 1.0.0 }
paths:
  /x:
    post:
      operationId: postX
      tags: [x]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              additionalProperties: true
              properties:
                status: { type: string }
      responses:
        '200':
          description: ok
          content:
            application/json: { schema: { type: object } }
